"use strict"; //treat all js code as newer version

// alert() --> we are using nodejs not browser ,it can't run like that different syntax

// weakly typed language incompetable data can be assigned
let a = "amar"
a = 4
// console.log(a)
//DataTypes in JS
/* primitive
1) number ==> range in 2^53
2) bigint ==> for large numbers
3) string ==> "sequence of charaters" (prefer -" ")
4) boolean ==> true/false
5) undifined ==> value not assigned to variable(type == object)
6) null ==> stand alone value(type == object)
7) symbols ==> uniqueness(used in react component)

non-primitives
1) object
*/ 

console.log(typeof null)